def decision_tree(input_features):
    if input_features[0] == 1:
        return "Class A"
    else:
        return "Class B"
feature_value = int(input("Enter a feature value (0 or 1): "))
input_features = [feature_value]
prediction = decision_tree(input_features)
print("Prediction:", prediction)
